@{
    ViewData["Title"] = "Attach Supporting Documents - Lecturer";
}

<div class="container py-4">
    <h3 class="mb-3">Attach Supporting Documents</h3>
    <p class="text-muted">Prototype upload — no files are stored.</p>

    <div class="mb-3">
        <label class="form-label">Select files</label>
        <input type="file" class="form-control" multiple />
    </div>
    <button class="btn btn-outline-primary" type="button">Upload (Prototype)</button>
    <a asp-action="Index" class="btn btn-link">Back</a>
</div>


